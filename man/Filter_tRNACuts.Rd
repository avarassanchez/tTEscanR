% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/GeneralProcessing.R
\name{Filter_tRNACuts}
\alias{Filter_tRNACuts}
\title{Filter Out Conditions With Low tRNA Cuts}
\usage{
Filter_tRNACuts(data, cutoff = 5000)
}
\arguments{
\item{data}{A \code{matrix} or \code{data.frame} of tRNA gene expression data, with tRNA genes as rows and conditions as columns.}

\item{cutoff}{Numeric; minimum total number of tRNA cuts required to retain a condition in \code{data}. Defaults to 5000.}
}
\value{
A filtered \code{matrix} or \code{data.frame} with tRNAs below the cutoff removed.
}
\description{
This function filters a tRNA expression matrix by removing conditions (columns) that fall below a specific total read count (\code{cutoff}).
It is useful for eliminating low-quality or poorly sequenced conditions that may bias downstream analyses.
}
\examples{
data(default_tTEscanR_tRNA_data)
tRNA_data_filtered <- Filter_tRNACuts(data = default_tTEscanR_tRNA_data, cutoff = 5000)
}
