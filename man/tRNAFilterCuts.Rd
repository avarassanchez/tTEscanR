% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/GeneralProcessing.R
\name{tRNAFilterCuts}
\alias{tRNAFilterCuts}
\title{Filter Out Conditions With Low tRNA Cuts}
\usage{
tRNAFilterCuts(data, cutoff = 5000, verbose = TRUE)
}
\arguments{
\item{data}{A \code{matrix} or \code{data.frame} of tRNA gene expression
data, with tRNA genes as rows and conditions as columns.}

\item{cutoff}{Numeric; minimum total number of tRNA cuts required to retain
a condition in \code{data}. Defaults to 5000.}

\item{verbose}{Logical; if \code{TRUE}, displays information messages.
Defaults to \code{TRUE}.}
}
\value{
A filtered \code{matrix} or \code{data.frame} with tRNAs below the
cutoff removed.
}
\description{
This function filters a tRNA expression matrix by removing conditions
(columns) that fall below a specific total read count (\code{cutoff}). It
is useful for eliminating low-quality or poorly sequenced conditions that
may bias downstream analyses.
}
\examples{
data("default_tTEscanR_tRNA_data", package = "tTEscanR")

tRNA_data_filtered <- tRNAFilterCuts(
    data = default_tTEscanR_tRNA_data,
    cutoff = 5000
)
}
